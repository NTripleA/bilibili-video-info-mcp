SESSDATA=
DedeUserID=
BILI_JCT=
PORT=8000

# Extra Host and Origin allowlist entries for remote_server.py (comma-separated).
# Set these to your deployment's public endpoint in your private environment.
# Loopback entries remain allowed; DNS rebinding protection stays enabled.
# MCP_ALLOWED_HOSTS=mcp.example.com,mcp.example.com:443
# MCP_ALLOWED_ORIGINS=https://mcp.example.com
